# shellcheck shell=bash

# Global
alias ll='ls -la'

if [[ "$(uname)" == "Linux" ]]; then
  # Linux
  alias clip='xclip -sel clip'
elif [[ "$(uname)" == "Darwin" ]]; then
  # MacOS
  alias finder='open -R'
fi

# Applications
alias ap='ansible-playbook'
alias py='python'
